export enum RoutesPath {
  HOME = '/',
  CHARACTERS = '/characters',
  CHARACTERDETAIL = '/characters/:id',
  PLANETS = '/planets',
}

export enum RoutesNames {
  HOME = 'Accueil',
  CHARACTERS = 'Personnages',
  CHARACTERDETAIL = 'Detail du personnage',
  PLANETS = 'Planètes',
}
